#include <bits/stdc++.h>
using namespace std;
typedef pair<int, int> par;

int main() {
    ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    int n, m;
    cin >> n >> m;
    for ()

    return 0;
}