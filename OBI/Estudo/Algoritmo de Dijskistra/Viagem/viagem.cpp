#include <bits/stdc++.h>
using namespace std;
typedef pair<int, int> par;

int main() {
    int v, n, m;
    cin >> v >> n >> m;
    vector<pair<int,int>> grafo(n, vector<pair<int,int>>(n, {0,0}));
    priority_queue<par, vector<par>, greater<par>> fila;

}