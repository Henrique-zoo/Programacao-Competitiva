#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m, aux;
    vector<int> h, m;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> aux;
        h.push_back(aux);
    }
    cin >> m;
    for (int i = 0; i < m; i++) {
        cin >> aux;
        m.push_back(aux);
    }
    sort(h.begin(), h.end());
    sort(m.begin(), m.end());

    int i = -1, j = -1;
    while (j < n-1) {
        if (h[i] )

    } 
}