#include <bits/stdc++.h>
using namespace std;
 
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, a, b, c, d, k;
    cin >> n;
    while (n--) {
        cin >> a >> b >> c >> d;
        if (b > 6) {
            k = 12 - b;
        }
    }
    return 0;
}